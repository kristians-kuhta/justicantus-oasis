{
  "name": "pinFile",
  "version": "1.0.0",
  "description": "Encrypts and pins a file to IPFS",
  "main": "index.js",
  "scripts": {
    "start": "INFURA_API_KEY=$INFURA_API_KEY INFURA_API_SECRET=$INFURA_API_SECRET ALLOWED_ORIGINS=* npx functions-framework --target=pinFile --source=index.js --port=8081",
    "test": "mocha test/*.js"
  },
  "license": "MIT",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "axios": "^1.4.0",
    "busboy": "^1.6.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "sinon": "^15.2.0"
  }
}
